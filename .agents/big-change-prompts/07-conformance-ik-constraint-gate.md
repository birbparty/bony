# /big-change prompt - conformance (IK constraint golden gate)

> **How to use this in a later session**
> 1. `cd /Users/punk1290/git/bony`
> 2. Paste everything below the line into the session.
> Tell the agent: *all four inputs (Description, Links, Success Criteria,
> Constraints) are provided below - treat Questions 1-4 as pre-answered, echo
> them back once for a single confirmation, then proceed to auto-detection and
> generate the plan.*
>
> **Order:** this is **step 3 of 3**. Depends on step 2 making the bony CLI emit
> and round-trip IK constraints. Run last.
> **Candidate category:** frontier.

---

/big-change Add cross-runtime conformance coverage for IK constraints: a rig asset, numeric golden, binary fixture, input script, and gate wiring.

You already have all four `/big-change` inputs below - treat Questions 1-4 as
pre-answered, echo them back for one confirmation, then proceed.

**Description**

Steps 1-2 made IK constraints expressible and runtime-evaluable in the Nim
reference runtime. This slice makes IK part of the cross-runtime contract by
adding a dedicated M5 IK conformance rig and committing its goldens, exactly as
the existing M5 path rig (`m5_rig`) and the M9 second-asset precedent
(`m9_non_scalar_rig`) do. IK lives in the M5 band, so name the asset
`m5_ik_rig` to sit alongside `m5_rig` without renumbering milestones.

Follow the documented "Adding a new milestone" recipe in
`conformance/README.md` (create rig -> generate `.bnb` -> generate numeric
golden -> create input script -> commit -> verify gates).

**Links to Relevant Documentation**
- Clean room: `docs/CLEANROOM.md`
- Provenance: `docs/PROVENANCE.md`
- Comparable research: `docs/comparable-feature-set.md`
- Conformance suite docs: `conformance/README.md`
- Numeric-golden format and tolerance (`bony.numeric-golden.v1`, `1e-4`):
  `conformance/README.md`, `docs/float-math-contract.md`
- Input-script schema: `spec/bony-input-script.schema.json`
- Existing M5 path rig (style template): `conformance/assets/m5_rig.bony`,
  `conformance/assets/bnb/m5_rig.bnb`, `conformance/goldens/m5_rig_t0.json`,
  `conformance/scripts/m5_sample.json`
- Second-asset-in-band precedent: `conformance/assets/m9_non_scalar_rig.bony`,
  `conformance/goldens/m9_non_scalar_rig_t0.json`
- CI gate runners: `scripts/ci/conformance_run.py`,
  `scripts/ci/round_trip_run.py`, `scripts/ci/input_script_run.py`,
  `scripts/ci/suite_run.py`, `scripts/ci/schema_validate_assets.py`
- CLI commands: `cli/README.md` (`json-to-bnb`, `golden-gen`, `play`)
- Step 2 output: a bony CLI binary that emits and round-trips IK constraints.

**Current Local Facts To Preserve**
- The numeric-golden gate (`scripts/ci/conformance_run.py`) runs `.bony` and the
  matching `.bnb` against the same `*_t0.json` golden within `1e-4`. The IK rig
  must pass for both extensions, proving binary IK parity.
- The round-trip gate (`scripts/ci/round_trip_run.py`) globs `*_rig.bnb`; naming
  the binary fixture `conformance/assets/bnb/m5_ik_rig.bnb` enrolls it
  automatically. Generate it with the step-2 CLI so its canonical bytes are
  current.
- Image goldens are Nim-only and not part of the cross-runtime contract; an
  IK `*_play.png` is optional and may be left `pending` in the README table like
  M7/M9, unless the reference rasterizer renders the rig cleanly.
- Goldens must be generated by the step-2 CLI, never hand-edited. Treat them as
  generated artifacts.

**Success Criteria**
- `conformance/assets/m5_ik_rig.bony` exists: a small rig exercising one-bone,
  two-bone, and chain IK, validating against `spec/bony.schema.json` and
  `spec/bony-input-script.schema.json` where relevant. **The rig must be authored
  so the IK solve visibly changes the pose at `t=0`** - place each IK target bone
  offset from where the unconstrained chain tip would naturally rest, so the
  `m5_ik_rig_t0.json` golden differs from the unconstrained pose and the gate
  cannot pass trivially. A setup-pose that coincides with the unconstrained pose
  does not exercise the solver and is unacceptable.
- `conformance/assets/bnb/m5_ik_rig.bnb` is generated by the step-2 CLI
  (`json-to-bnb`).
- `conformance/goldens/m5_ik_rig_t0.json` is generated by `golden-gen` at `t 0.0`
  and reflects the solved IK pose.
- `conformance/scripts/m5_ik_sample.json` drives the setup pose **and** at least
  one nonzero-time sample that animates an IK target bone, with its golden, so the
  gate exercises the solver at more than `t=0` (not just the offset rest pose).
- `conformance/README.md` milestone table documents the new IK rig under M5. (The
  M9 non-scalar rig is already listed in both tables - confirm it still is; no new
  M9 row is needed.)
- All gates pass for both `.bony` and `.bnb`:

```bash
nim c --path:runtime-nim/src -o:/tmp/bony_bin cli/bony_cli.nim
python3 scripts/ci/schema_validate_assets.py --bony-bin /tmp/bony_bin
python3 scripts/ci/conformance_run.py   --bony-bin /tmp/bony_bin
python3 scripts/ci/round_trip_run.py    --bony-bin /tmp/bony_bin
python3 scripts/ci/input_script_run.py  --bony-bin /tmp/bony_bin
python3 scripts/ci/suite_run.py         --bony-bin /tmp/bony_bin
cd runtime-dart && dart test && cd ..
```

- `runtime-dart` still passes after the new fixtures land (Dart loads the IK
  asset model even though it defers evaluation).

**Constraints**
- Preserve clean-room posture: do not inspect or derive from DragonBones,
  Spine, Rive, Live2D, or Lottie runtime source, importer source, generated
  definitions, exact wire layouts, type/property keys, or copied docs prose. The
  rig geometry must be original and authored for this test, not derived from any
  third-party sample asset.
- Use `docs/comparable-feature-set.md` only for capability categories.
- Keep Rive importer work out of scope.
- Keep Spine importer work blocked for human/legal review.
- Do not hand-edit generated goldens or `.bnb` bytes; regenerate them with the CLI.
- IK only - no transform/physics constraint assets.
- Do not expand the input-script schema unless a concrete failure proves the
  existing schema cannot express the IK target animation.
- Keep the slice small enough for one meaningful implementation session.
