# bony-8i1.9 — M5 transform-constraint final verification (prompt-09 slice)

Final gate for the Nim-reference transform-constraint milestone (epic bony-8i1).
All gates run fresh on the iteration-159 branch off `main`. This bead unblocks the
Dart parity follow-up (prompt 10).

## Gate results — ALL PASS

| # | Gate | Command | Result |
|---|------|---------|--------|
| 1 | Nim compile | `nim check --hints:off --path:runtime-nim/src runtime-nim/src/bony.nim` | ✅ exit 0, clean |
| 2 | Codegen drift | `python3 codegen/generate.py --check` | ✅ exit 0 (sources ↔ generated agree) |
| 3 | Codegen unit tests | `python3 -m unittest discover -s codegen -p 'test_*.py'` | ✅ OK |
| 4 | Schema-validate assets | `python3 scripts/ci/schema_validate_assets.py` | ✅ 10 passed, 0 failed |
| 5 | Repo gate | `make test` | ✅ exit 0 |
| 6a | Nim smoke tests | `nim c -r tests/test_smoke.nim` | ✅ exit 0 (200 checks incl. transform eval/round-trip/ordering) |
| 6b | Nim IK tests | `nim c -r tests/test_ik_current_pivot.nim` | ✅ exit 0 |
| 6c | Nim CLI tests | `nim c -r tests/test_cli_pose.nim` | ✅ exit 0 |
| 7 | Conformance suite | `python3 scripts/ci/suite_run.py --bony-bin <cli>` | ✅ PASS — 4 gates (numeric-golden, image-golden, input-script, round-trip), 0 skipped |

## Golden reproduced from BOTH sources

`m5_transform_rig_t0.json` regenerates **bit-identically** from:
- `conformance/assets/m5_transform_rig.bony` (JSON loader), and
- `conformance/assets/bnb/m5_transform_rig.bnb` (binary loader, 215 bytes, non-empty).

Both equal the committed golden. The JSON and binary loaders agree; the golden is
not vacuous at empty.

## Non-vacuous delta (≫ 1e-4)

Constrained bone `constrained`, unconstrained FK pose vs solved (t=0):

- Unconstrained: `tx=40, ty=0`, identity basis (`a=1, b=0, c=0, d=1`).
- Solved: `tx=60, ty=30, a≈1.38582, b≈0.57403, c≈-0.60876, d≈0.79335`
  (rotation 22.5°, scaleX 1.5, scaleY 1.0, shearY 15° — each the exact 0.5
  midpoint to the target).
- Translation delta: `hypot(20, 30) ≈ 36.0555` skeleton units.
- Basis delta: `|a-1| ≈ 0.3858`, `|c-0| ≈ 0.6088`.

Every channel (translate/rotate/scale/shear) is load-bearing and the delta is
four orders of magnitude above the `1e-4` conformance tolerance.

## Milestone surface (all landed, Nim reference)

registry+defaults+codegen (8i1.2) → Nim model (8i1.3) → JSON/BNB/CLI load path
(8i1.4) → runtime eval (8i1.5) → conformance asset+golden (8i1.7) → unit tests
(8i1.6) → docs (8i1.8). Grounding: [[8i1.1-transformconstraint-grounding]].

## Scope boundary

Nim reference only. Dart transform-constraint evaluation is the prompt-10 parity
follow-up; Dart's static conformance asset list intentionally omits
`m5_transform_rig` until then. The setup-pose golden is the cross-runtime gate
that parity work must reproduce within `1e-4`.
